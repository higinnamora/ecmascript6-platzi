// Esto es lo que se implemento en ecmascript 8

// Includes Nos permite saber si hay un elemento dentro de este
let numbers = [1, 2, 3, 7, 8];

if (numbers.includes(7)) {
  console.log('Si se encuentra el valor 7');
} else {
  console.log('No se encuentra')
}

// Elevar a la potencia con dos **

let base = 4;
let exponent = 3;
let result = base ** exponent;

console.log(result)